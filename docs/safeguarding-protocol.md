# Safeguarding Protocol

Self-harm: detect-don't-block, response template, escalation path, helpline copy per language, staff protocol, access restrictions. **Clinical/safeguarding review required.**

> Status: partial — S-42 ownership and two constraints recorded below; the protocol itself is not yet written

---

## TODO — recorded ahead of writing

- When this doc is written it **owns** the S-42 constraints (never auto-open, never in case history, never reachable by accidental tap); the `docs/navigation-ia.md` S-42 row then shrinks to a pointer here.
- (a) "Never auto-open" must still permit the detect-don't-block response to **offer** S-42 without force-navigating.
- (b) "Never appear in case history" means absent from **farmer-visible** history only — signal detection is still retained per root `CLAUDE.md` #3.
